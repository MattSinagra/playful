<div class="page-header text-center">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col col-10">
                <h1>{{ title ?: post.title }}</h1>
                <div class="tagline">{{fn('get_bloginfo', 'description')}}</div>
            </div>
        </div>
    </div>
</div>